import React from 'react'

const Home = () => (
  <div>
    <h3>Thanks for visiting - Check the examples from the menus</h3>
    
    <p>Check out the examples for Virtual Reality, 3D models, 3D UI controls, Skybox and mesh creations using BabylonJS on WebGL running in a React App.</p>

    <p>Interesting part is the UI 2D, where a WebGL texture is dynamically updating from props - best experienced in VR mode.</p>
    
    <p className="text-muted small">More coming soon...</p>
  </div>
)

export default Home